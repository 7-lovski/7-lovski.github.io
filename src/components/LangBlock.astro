---
import LocalizedProseBlock from './LocalizedProseBlock.astro';

interface Props {
	lang: 'zh' | 'en';
}

const { lang } = Astro.props;
---

<LocalizedProseBlock lang={lang}>
	<slot />
</LocalizedProseBlock>
